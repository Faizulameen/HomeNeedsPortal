<div class="card">
  <div class="card-body">
    <h2 class='card-title d-flex justify-content-between mb-4'></h2>


<h1>Edit Review payment</h1>
<%= simple_form_for([@product, @payment]) do |f| %>
	
     <div class="field">
    <%= f.label :status %>
    <%= f.select :status, Payment::STATUS_OPTIONS %>
      </div>

  <%= f.button :submit %>
<% end %>
</div>
</div>